#include <stdio.h>

int main()
{
    int a,b;

    printf("Ingrese a a: ");
    scanf("%d", &a);
    printf("Ingrese a b: ");
    scanf("%d", &b);

    if(a!=b)
        printf("a es diferente de b")
    else if(a==b)
        printf("a es igual a b")
    if(a<b)
        printf("a es mayor a b")
    if(a>b)
        printf("a es mayor a b");
    @
    return 0;
    ~~
}